[project]
name = "agent"
version = "0.1.0"
description = "Autonomous AI agent with MCP tools integration"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio>=6.2.0",
    "mcp>=1.25.0",
    "ollama>=0.4.0",
    "requests>=2.31.0",
]
